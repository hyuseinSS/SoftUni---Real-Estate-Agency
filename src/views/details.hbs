<main>
  <section id="deatils-page">
    <div class="wrapper">
      <div class="product-img">
        <img src="{{house.image}}" />
      </div>
      <div class="product-info">
        <div class="product-text">
          <div class="product-text">

            <h1>Name: {{house.name}}</h1>
            <h2>Type: {{house.type}}</h2>
            <h4>Year: {{house.year}}</h4>
            <h4>City: {{house.city}}</h4>
            <p>Description: {{house.description}}</p>
            {{#if user}}
            {{#if house.rentedBy}}
              <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
              <p>People rented this housing: {{house.result}}</p>
            {{else}}
              <!-- If not display: -->
              <p>People rented this housing: There are no tenants yet.</p>
            {{/if}}
          </div>
        </div>

        <!-- if there is no registered user, do not display buttons-->
        <div class="product-btn">
          <!-- Only for registered user and creator of the housing offer-->
          {{#if house.isOwned}}
            <a href="/housing/edit/{{house._id}}" class="edit">Edit</a>
            <a href="/housing/delete/{{house._id}}" class="remove">Delete</a>
            <!-- logged in user with available pieces-->
          {{/if}}

          {{#if house.isAlreadyRented}}

            <p class="alRentHome">You have already rented this home</p>
          {{else}}
          {{#unless house.isOwned}}
            {{#if house.availability}}
              <a href="/housing/rent/{{house._id}}" class="rentHome">Rent a
                home, available
                {{house.availability}}
                housing</a>
            {{else}}
              <!-- logged in user with no available pieces-->
              <p class="no-housing">No Housing Available!</p>
            {{/if}}
            {{/unless}}
          {{/if}}
          {{/if}}

        </div>
      </div>
    </div>
  </section>
</main>